// Local development only — copy config.example.js to config.js and fill in values.
// On Vercel the real values come from /api/config (env vars).
// This file is gitignored after initial scaffold.
export const SUPABASE_URL  = '';
export const SUPABASE_ANON = '';
