{
  "name": "libra",
  "version": "0.5.6",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "echo 'Static site — no build step required'",
    "check:inline": "node scripts/check-no-inline-scripts.mjs"
  },
  "type": "module"
}
